@model Bakers.Models.ViewModels.RoleViewModel

@{
    ViewData["Title"] = "Wijzigen rollen";
}

@*<h1>Wijzigen Rollen</h1>
<hr />*@
<div class="row"style="margin-top: 20px;">
    <div class="col-md-4">
        <form asp-action="Edit"style="display: flex; flex-direction: column; gap: 8px;">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="UserName" />
            <div class="form-group">
                @Html.DisplayNameFor(model => model.UserName)
                @Html.DisplayFor(model => model.UserName)
            </div>
             <div>
                <label asp-for="Roles" class="control-label"></label>
                <select multiple="multiple" asp-for="Roles" class="form-control" asp-items="ViewBag.RoleIds"></select>
            </div>
            <div class="form-group">
                <input style="border: none; border-radius: 4px; background-color: #DDB7B2; color: white; width: 100px" type="submit" value="Bewaren" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div class="mt-3">
    <a style="text-decoration: none; color: black; cursor: pointer;" asp-action="Index">⬅ Back to List</a>
</div>
